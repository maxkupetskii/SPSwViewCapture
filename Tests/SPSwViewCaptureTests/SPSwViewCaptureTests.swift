import XCTest
@testable import SPSwViewCapture

final class SPSwViewCaptureTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SPSwViewCapture().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
